import SalonManagement from '@/components/SalonManagement';

export const metadata = {
  title: 'Salon Management - SalonSync',
  description: 'Manage salon WhatsApp connections and QR codes',
};

export default function SalonManagementPage() {
  return <SalonManagement />;
} 